
<div class="p-4 p-md-5 mb-4 rounded justify-content-center text-body-emphasis">
    <div class="col-lg-6 px-0">
      <h1 class="display-4 text-primary fst-italic bg-image">Welcome to the Gossip Project, <%= @first_name %> !</h1>
      <p class="lead my-3">Le lieu de tout les potins qui concernent tes camarades de THP.</p>
      <p class="lead text-primary mb-0"><a href="https://www.thehackingproject.org/?locale=fr" target="blank" class=" fw-bold">The Hacking Proejct</a></p>
    </div>
  </div>
 
<div class="new_gossip_link">
<p><%= link_to "Créer un nouveau gossip", new_gossip_path, class: "link_gossip mb-0 fw-bold" %></p>
</div>

<div class="container">
  <div class="row">
    <% @gossips.each do |gossip| %>
      <div class="col-md-4">
        <div class="my-3 p-3 bg-white rounded box-shadow">
          <h6 class="border-bottom border-gray pb-2 mb-0"> <%= gossip.user.first_name %> </h6>  
          <p class="media-body pb-3 mb-0 small lh-125 border-bottom border-gray">
            <strong class="d-block text-gray-dark"><%= gossip.title %></strong>
            <%= gossip.content.truncate(20, omission: '...') %> <%= link_to "Voir plus", gossips_path(gossip.id) %>

          </p>
        </div>
      </div>
    <% end %>
  </div>
</div>
