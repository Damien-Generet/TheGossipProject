<h1>Lâche ton doss'</h1>

<% if @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à mettre à jour le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>


<%= form_tag url_for(action: 'update'), method: "patch" do %>
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">

  <%= label_tag 'Quel est le contenu?' %>
  <p class="mt-5">Titre :</p>
  <%= text_field_tag :comment, @gossip.title, autocomplete: "off", class: "form-control", name: 'new_title'%>
  </br>
  <p>Gossip :</p>
  <%= text_area_tag :comment, @gossip.content, autocomplete: "off", class: "form-control",  name: 'new_content'%>
  <br>

  <%= submit_tag "Mettre à jour le potin" %>

<% end %>




