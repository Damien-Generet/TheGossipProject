<h1 class="text-primary px-5 d-flex m-3 justify-content-center bg-image">WHAT A CRUNCHY GOSSIP !</h1>


<% if @gossip.errors.any? %>
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
  <ul>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
  <%end%>
      
<div class="container container_create">
    <%= form_tag url_for(action: 'create'), method: "post" do %>
    <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
    <div class="content_gossip">
      <%= label_tag '' %>
      <%= text_field_tag 'title', nil, placeholder: 'Title' %></br>
      <%= label_tag '' %>
      <%= text_area_tag 'content', nil, placeholder: 'Write your crunchy gossip...'%>
      <h4 class="text-primary px-5 d-flex m-3 justify-content-center bg-image">Choose some Hashtag</h4>
    </div>
    <div class="tag_gossip">
      <ul>
        <% @tags.each do |tag| %>
            <li>
              <%= check_box_tag 'array_tag[]', tag.id -%>
              <%= h tag.title -%>
            </li>
        <% end %>
      </ul>
    </div>

    <div class="input_class">
      <%= submit_tag "Créer un potin", class: 'btn btn-primary m-4' %>
    </div>
    <% end %>
</div>